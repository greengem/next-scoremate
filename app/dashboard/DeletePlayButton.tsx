import { Button } from "@nextui-org/button";

export default function DeletePlayButton({
    playId
}: {
    playId: string;
}) {
    return (
        <Button>
            Delete
        </Button>
    )
}